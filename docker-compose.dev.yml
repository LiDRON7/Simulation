services:
  drone-dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    container_name: drone_sim_dev
    network_mode: host
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
      - XAUTHORITY=/tmp/.docker.xauth
      - GZ_SIM_RESOURCE_PATH=/drone_sim/models:/drone_sim/worlds
      - LIBGL_ALWAYS_SOFTWARE=1
      - MESA_GL_VERSION_OVERRIDE=3.3
      - GALLIUM_DRIVER=llvmpipe
      - NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES:-void}
    runtime: ${DOCKER_RUNTIME:-runc}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /tmp/.docker.xauth:/tmp/.docker.xauth:rw
      - ./models:/drone_sim/models
      - ./worlds:/drone_sim/worlds
      - ./config:/drone_sim/config
      - ./scripts:/drone_sim/scripts
      - ./ros2_ws:/drone_sim/ros2_ws
    devices:
      - /dev/dri:/dev/dri

  px4:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    container_name: drone_px4
    network_mode: host
    environment:
      - PX4_SIM_HOST_ADDR=127.0.0.1
      - PX4_GZ_STANDALONE=1 # don't spawn Gazebo, connect to existing instance
    volumes:
      - ./models:/drone_sim/models
      - ./worlds:/drone_sim/worlds
    depends_on:
      - drone-dev
    command: >
      bash -c "
        source /opt/ros/jazzy/setup.bash &&
        source /px4/Tools/simulation/gz/setup_gz.bash &&
        sleep 5 &&
        cd /px4 && ./build/px4_sitl_default/bin/px4
        -s ./ROMFS/px4fmu_common/init.d-posix/px4-rc.simulator
      "
